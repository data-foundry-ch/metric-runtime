project:
  name: metric-runtime
  description: Default project configuration for local development

catalog: {}

runtime:
  evaluation_interval: 15m
  default_profile: local

investigation:
  max_depth: 5
  min_support: 100

state:
  detections_before_open: 2
  resolve_after_healthy_windows: 2
  cooldown: 30m
  min_impact_eur: 50
